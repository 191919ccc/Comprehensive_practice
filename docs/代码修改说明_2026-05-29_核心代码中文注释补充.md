# 核心代码中文注释补充说明

## 修改目的

本次修改的目标是提高项目代码可读性，方便答辩、交接和后续维护。注释重点放在系统数据流、模型训练口径、告警规则、前端展示口径和后端聚合接口上，而不是对每一行代码做机械翻译。

## 修改文件

- `frontend/shared-ui.js`
- `frontend/dashboard-home.js`
- `frontend/dash-pages.js`
- `python/common/config.py`
- `python/ml/train_daily_predict.py`
- `python/ml/stock_ml.py`
- `python/spark/stock_streaming_job.py`
- `java-backend/src/main/java/com/recruitment/backend/service/DashboardService.java`

## 具体修改

1. 前端公共工具增加中文注释
   - 说明 `StockShared` 的作用。
   - 说明 API 请求、Markdown 安全清洗、模型基线、告警触发原因和股票历史导出 URL 的统一口径。

2. 首页大屏增加中文注释
   - 说明首页负责 KPI、主图、告警摘要、模型状态、历史记录、导出 CSV 和 AI 助手。
   - 说明刷新锁、主图切换、模型状态指标、告警摘要、股票历史和导出功能的设计原因。

3. 子页面脚本增加中文注释
   - 说明 `alerts/models/market/system` 等页面共用同一脚本。
   - 说明告警等级兜底、单条告警触发原因、证据面板、告警统计图和模型页数据时效提示。

4. Python 配置与训练链路增加中文注释
   - 说明 `.env` 加载优先级、告警冷却窗口、日线数据时效校验和告警置信度过滤口径。
   - 说明训练入口使用日线数据、按模型阈值准备数据、阈值实验只用于解释、正式写库才执行漂移检测。

5. 机器学习核心逻辑增加中文注释
   - 说明最优参数配置、数据准备顺序、重复价格清洗、按阈值缓存数据集、RF 训练成本来源、LightGBM 生产定位。
   - 说明 `predicted_signal` 和 `alert_signal` 的区别，避免漂移检测继续被告警过滤信号污染。
   - 说明 ensemble 动态加权和预测结果写库的表设计口径。

6. Spark 与后端聚合服务增加中文注释
   - 说明 Kafka JSON 解析、历史基线阈值、重复行情过滤、聚合心跳、告警规则、冷却去重和微批处理顺序。
   - 说明后端聚合接口、告警查询、告警趋势/统计、股票排行和模型指标返回逻辑。

## 验证结果

已执行并通过：

```powershell
node --check frontend\vendor-loader.js frontend\shared-ui.js frontend\dashboard-home.js frontend\dash-pages.js
D:\anaconda3\envs\MachineLearn\python.exe -m py_compile python\common\config.py python\spark\stock_streaming_job.py python\ml\train_daily_predict.py python\ml\stock_ml.py python\tests\test_stock_utils.py
mvn -q -DskipTests compile
```

## 剩余限制

- 本次只补充注释，不调整业务逻辑。
- `java-backend/src/main/java/com/recruitment/backend/service/DashboardService.java` 中仍有部分历史乱码注释残留；本次已保证编译通过，后续可以单独做一次编码清理。
- `py_compile` 可能生成 `__pycache__`，缓存不影响运行；本次缓存清理命令被工具审批拒绝，未继续绕过执行。
